sources:
    _mapzen:
        type: TopoJSON
        url: https://tile.mapzen.com/mapzen/vector/v1/all/{z}/{x}/{y}.topojson?api_key=mapzen-AvWa8DQ # We are importing just the earth layer

layers:
    _earthLayer:
        data: # Notice we removed 'layer: earth' from this section
            source: _mapzen
        draw:
            polygons:
                order: 0
                color: '#ddeeee'
